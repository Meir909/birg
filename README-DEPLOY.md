# 🚀 Развертывание Birge на Vercel

## Подготовка проекта ✅
Проект уже готов к деплою:
- Все пути исправлены для работы в корневой директории
- Создан `vercel.json` для правильной маршрутизации
- Добавлен `package.json` для метаданных

## Способ 1: Через Vercel CLI (рекомендуется)

### 1. Установка Vercel CLI
```bash
npm i -g vercel
```

### 2. Логин в Vercel
```bash
vercel login
```

### 3. Деплой из папки проекта
```bash
cd "c:\Users\nurmi\OneDrive\Desktop\birge"
vercel
```

Ответьте на вопросы:
- Link to existing project? **No**
- What's your project's name? **birge**
- In which directory is your code located? **./**
- Want to override the settings? **No**

Готово! Ваш сайт будет доступен на `https://birge.vercel.app`

## Способ 2: Через GitHub (для автоматических обновлений)

### 1. Создайте репозиторий на GitHub
```bash
git init
git add .
git commit -m "Initial commit: Birge platform"
git branch -M main
git remote add origin https://github.com/yourusername/birge.git
git push -u origin main
```

### 2. Подключите к Vercel
1. Зайдите на [vercel.com](https://vercel.com)
2. Нажмите "Add New..." → "Project"
3. Выберите ваш GitHub репозиторий
4. Оставьте настройки по умолчанию
5. Нажмите "Deploy"

## Способ 3: Drag & Drop (самый простой)

1. Сожмите папку `birge` в ZIP архив
2. Зайдите на [vercel.com](https://vercel.com)
3. Перетащите ZIP архив на страницу
4. Дождитесь окончания деплоя

## ⚙️ Настройки после деплоя

### Переменные окружения (если нужны)
В настройках проекта Vercel добавьте:
- `NEXT_PUBLIC_GEMINI_API_KEY` (если используете серверный ключ)

### Кастомный домен
1. В настройках проекта Vercel → "Domains"
2. Добавьте ваш домен
3. Настройте DNS записи по инструкциям Vercel

## 🔧 Проверка после деплоя

1. **Главная страница**: `https://your-domain.vercel.app`
2. **AI-ассистент**: `https://your-domain.vercel.app/ai-assistant.html`
3. **Админ панель**: `https://your-domain.vercel.app/admin.html`
4. **Проверьте консоль** на отсутствие ошибок 404

## 🐛 Частые проблемы

### 404 ошибки
- Убедитесь что `vercel.json` в корне проекта
- Проверьте что все файлы загружены

### Стили не применяются
- Проверьте пути в `href="style.css"`
- Откройте DevTools → Network для проверки загрузки CSS

### JavaScript не работает
- Проверьте пути в `src="components.js"` и `src="gemini.js"`
- Убедитесь что нет ошибок в консоли

## 🔄 Автоматические обновления

При деплое через GitHub:
1. Сделайте изменения: `git add . && git commit -m "Update"`
2. Отправьте: `git push`
3. Vercel автоматически задеплоит изменения

## 📱 Мобильная версия

После деплоя проверьте на мобильных устройствах:
- Адаптивность дизайна
- Работа тач-интерфейса
- Скорость загрузки

---

🎉 **Поздравляю! Ваш проект Birge готов к деплою на Vercel!**

Если возникнут проблемы, проверьте консоль браузера и логи деплоя в Vercel.
